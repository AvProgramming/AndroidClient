<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.restaurant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Restaurant"
            android:usesCleartextTraffic="true">
        <activity
                android:name=".activities.ShowDetailActivity"
                android:exported="false" />
        <activity
                android:name=".activities.StartAnimActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".login.login.RegisterActivity"
                android:exported="false" />
        <activity
                android:name=".login.login.LoginActivity"
                android:exported="true"
                android:label="@string/title_activity_login" />
        <activity
                android:name=".activities.UpdatePurchaseActivity"
                android:exported="false"
                android:label="@string/title_activity_update_purchase"
                android:theme="@style/Theme.Restaurant.NoActionBar" />
        <activity
                android:name=".activities.CreateOrderActivity"
                android:exported="false" />
        <activity
                android:name=".activities.MainActivity"
                android:exported="false" />
    </application>

</manifest>